spring:
  application:
    name: copiou-papelaria-api

  datasource:
    url: jdbc:postgresql://localhost:5399/copiou_papelaria
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true

springdoc:
  api-docs:
    path: /api/api-docs
  swagger-ui:
    path: /swagger/index.html

copioupapelaria:
  security:
    secret: "shiiiiiu-is-a-secret-bro"